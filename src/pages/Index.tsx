
import { Navigate } from "react-router-dom";
import HomePage from "./HomePage";

const Index = () => {
  // Simply redirect to the HomePage component
  return <HomePage />;
};

export default Index;
